# API Configuration
# The server exposes its REST API under /api
# Make sure to include /api in the base URL
VITE_API_BASE_URL=http://localhost:8000/api

# Optional: Development server port
# Avoid port 8000 which the server uses
PORT=3000
